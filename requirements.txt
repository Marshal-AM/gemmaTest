# Pipecat voice pipeline + API server
pipecat-ai[daily,silero]

# Gemma 4 multimodal model (install torch+torchvision separately — see scripts/setup.sh)
transformers>=5.16
accelerate
huggingface_hub
torchvision

# API server
fastapi
uvicorn
aiohttp
python-dotenv
loguru
pyngrok
openai

# Optional: vLLM backend (recommended for production speed)
# pip install "vllm[audio]"  — see scripts/install_vllm.sh
# For CPU-only dev: pip install torch torchvision
